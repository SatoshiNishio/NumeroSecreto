 Programacao
; = inserido em momento de interpretação
let = cria uma variável

Maycon, sua resposta está correta, parabéns! O problema no código disponibilizado por Luis é que o operador de comparação utilizado na estrutura condicional 'if' está incorreto. Ao invés de usar o operador de atribuição '=', que simplesmente atribui o valor da variável 'senhaDoSistema' à variável 'senha', o correto seria utilizar o operador de comparação '==', que verifica se os valores das duas variáveis são iguais. Dessa forma, a estrutura condicional ficaria: if (senha == senhaDoSistema) { alert("Acesso ao sistema garantido"); } Assim, o sistema de login só permitiria o acesso caso a senha digitada pelo usuário fosse exatamente igual à senha definida no sistema. Parabéns por ter identificado corretamente o problema no código! Continue estudando e praticando lógica de programação, você está no caminho certo para se tornar um ótimo programador.

Desafio
Mostre um alerta com a mensagem "Boas vindas ao nosso site!".

Declare uma variável chamada nome e atribua a ela o valor "Lua".

Crie uma variável chamada idade e atribua a ela o valor 25.

Defina uma variável numeroDeVendas e atribua a ela o valor 50.

Defina uma variável saldoDisponivel e atribua a ela o valor 1000.

Exiba um alerta com o texto "Erro! Preencha todos os campos"

Declare uma variável chamada mensagemDeErro e atribua a ela o valor "Erro! Preencha todos os campos" Agora exiba um alerta com o valor da variável mensagemDeErro.

Use um prompt para perguntar o nome do usuário e armazená-lo na variável nome.

Peça ao usuário para digitar sua idade usando um prompt e armazene-a na variável idade.

Agora, caso a idade seja maior ou igual que 18, exiba um alerta com a mensagem "Pode tirar a habilitação!".

